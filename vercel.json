{
  "version": 2,
  "buildCommand": "./build_files.sh",
  "installCommand": "pip install -r requirements.txt",
  "builds": [
    { "src": "django_projeto_carros/app/wsgi.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "django_projeto_carros/app/wsgi.py" }
  ]
}
